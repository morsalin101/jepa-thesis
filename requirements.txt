# Local dev + Kaggle. Kaggle images already ship torch/cuda, so on Kaggle you
# usually DON'T reinstall torch (it's preinstalled and CUDA-matched).
# Pin only what you actually import.
numpy
tqdm
# torch  # uncomment for local install; on Kaggle it's already present
